# CLAUDE.md

## プロジェクト概要

このリポジトリは [omkbd/ErgoDash](https://github.com/omkbd/ErgoDash) のフォークです。
**目的: 既存の ErgoDash を改変して、キースイッチのソケット（ホットスワップ）対応版を作ること。**

ErgoDash は左右分離型の自作キーボードで、本リポジトリにはハードウェア設計データ一式が含まれます。ソフトウェアプロジェクトではなく、ビルド・テスト・サーバー起動といった概念はありません。

## リポジトリ構成

- `PCB/` — KiCad の基板設計データ（本プロジェクトの主な改変対象）
  - `Rev.1/`, `Rev.1.1/`, `Rev1.2/` — 各リビジョン。`Rev1.2` が最新（本家由来・無改変）
  - 各リビジョンに `.sch`（回路図）、`.kicad_pcb`（基板レイアウト）、`gerber/`（製造データ）、`library/`（部品ライブラリ）を含む
- `Case/` — ケースの図面データ（.ai / .eps / .svg / .pdf）
- `Firmware/` — VIA 対応ファームウェアの zip（QMK 本体は [qmk_firmware の ergodash ディレクトリ](https://github.com/qmk/qmk_firmware/tree/master/keyboards/ergodash) を参照）
- `Doc/` — ビルドガイド（日英）、ショップリスト
- `mini/` — 派生モデル ErgoDash mini のデータ一式（同様の構成）

## 面の対応（重要・全ての前提）

ビルドガイド `Doc/build.md` より、部品（Pro Micro 等）は基板の**裏側**に、スイッチは**表側（キーキャップ側）**に取り付ける。本データは U3・ダイオードが **F.Cu** にあるので：

- **KiCad の `F.Cu` ＝ 物理的な裏側**（部品・ソケット面。使用時に下を向く面）
- **KiCad の `B.Cu` ＝ 物理的な表側**（キーキャップ面。使用時に上を向く面）
- **ホットスワップソケットは部品と同じ裏側＝`F.Cu` に置く**（スイッチは表側 B.Cu から挿す）

KiCad で F.Cu を手前にして設計＝キーボードを裏から見ていることになり、**使用時のレイアウトは左右反転して見える**。この事実が下記のディレクトリ左右取り違え（修正済）の原因だった。

## Git 運用

- `origin` = tanakamanabu/ErgoDash（このフォーク）、`upstream` = omkbd/ErgoDash（本家）
- ソケット対応の作業は現在 `socket-front` ブランチで行っている（旧 `socket-pcb` ブランチの続き）。`master` は本家追従用
- 本家由来の既存リビジョン（Rev.1 / Rev.1.1 / Rev1.2）のデータは原則変更せず、ソケット対応版は別ディレクトリで作業する

### 左右別基板（2026-06-20 ディレクトリ左右を修正）

- `PCB/Rev1.2-socket-right/` — **右手基板。現在の主開発対象**。ソケットを表面化（F.Cu へフリップ）済みで、マトリクスを手配線中（WIP）
- `PCB/Rev1.2-socket-left/` — **左手基板の置き場所だが現状 stale**（旧フリップの右手データが入ったまま）。**有効な左手基板はまだ存在しない**。真の左手基板は独立に手配線して作る必要がある（下記ミラー不可のため）
- 当初は「左を正・右はミラー生成」方針だったが、(1) ディレクトリの左右が利き手と逆だった（F.Cu=裏側のため。2026-06-20 に中身をスワップ修正）、(2) ミラー生成自体が不可能と判明（下記）、により方針を変更。**左右は独立した別基板として手配線する**

## ソケット版の設計方針

### ミラー生成は不可能（2026-06-20 確定・旧方針を撤回）

- キーボードPCBの左右ペアは**純粋な鏡像では作れない**。実在の MX スイッチはキラル（鏡像版が存在しない）ため、基板をミラーすると物理的にスイッチが刺さらなくなる
- `Flip(LEFT_RIGHT)` は「同一基板を裏返しただけ」＝同じ手の基板にしかならない（別の手にならない）
- `FOOTPRINT.Mirror()` は KiCad 10.0.3 でネイティブクラッシュする
- → **各手を独立に手配線する**。旧 `PCB/mirror_right.py`（Flip 方式）は無効＝使用しない

### ソケットの表面化と配線やり直し

- 元のソケットデータはソケットが **B.Cu（表＝キーキャップ側＝誤り）**にあった。`PCB/flip_sockets_front.py`（KiCad 10 同梱 python で実行）で全ソケットを各自の中心でフリップし、SMD パッドを **F.Cu（裏側）**へ移した
- フリップに伴いスイッチのスルーホールが裏挿し用のミラー配置へ移動し、**マトリクス配線が外れる**（フリップ直後で約70箇所未配線）。配線は KiCad GUI で手配線し直す（pcbnew スクリプトでの自動ルートは不可・低品質）
- **SW35 / SW40 の回転**: フリップ後、親指クラスタ端の2キーでソケットの羽が基板外形をはみ出すため回転で基板内に収めた。**SW40（2U）→ +180°**（2U は長方形なので 180° のみ可）、**SW35（1U）→ +270°**（正方形なので 90° 刻み可）。`flip_sockets_front.py` に組込済

### 部品・回路の改変（本家からの削減）

- **LEDオプションは全廃**（スイッチ下3mm LEDバックライト・WS2812Bアンダーグローとも）。関連部品（LED、470Ω/1kΩ抵抗、MOSFET Q1、LED用コネクタJ2〜J5）も回路図・基板から削除
- ソケットは Kailh PG1511（MX互換）。**Alpsスイッチ対応は失われる**（許容済み）。フットプリントは各基板の `socket.pretty/`（kiswitch 由来、MIT/CC-BY-SA）。**Plated版**採用で、ソケットなしで直接ハンダ付けする組み立ても可能
- 回路図のキーマトリクス（スイッチ・ダイオード・Pro Micro・TRRS・リセットSW）は本家と同一を維持。変更検証はネットリスト照合で行う
- **スピーカーは削除**（2026-06-12）。M1 と直列1kΩの R40 を回路図・基板から削除（本家でも BOM 非掲載の隠しオプション）
- **I2C 接続オプションも削除**（2026-06-12）。プルアップ R1/R2 と TRRS 4線目を SCL につなぐジャンパ W1 を削除。左右通信は QMK のシリアル（split.serial pin D0 ＝ `/sda/uart` ネット）のみ。`/scl`・`/xtradata` はネット名として U3・TRRS のピンに残るが配線・部品は無し
- **旧オプション用の孤立シルクは削除済み**（V/G/RGB/I2C/Xtra/TX0/BL1k/Au1k）。基板上の文字は銘板2つ（`Designed by omkbd` / `ErgoDash Rev. 1.2 Socketed by tsubu`）のみ
- **親指キーの代替レイアウトは全廃**（2026-06-12）。2U は SW40 のみ、他は全部 1U。代替位置のシンボル SW36/SW38/SW39/SW41 は回路図から削除済み、旧 SW36 位置のスタビライザーも基板から削除済み
- **SW13/SW14 は 270°回転**で配置。Pro Micro (U3) がこの2キーの真下にあり、0°/180°ではソケットパッドが U3 のピン列と必ずショートするため、U3 の2つのピン列の間（約13mmの回廊）にソケットの羽を縦向きで通している（最小間隔0.27mm）

### 未解決・要検討

- **真の左手基板の作成**: ミラー不可のため独立に手配線が必要。`Rev1.2-socket-left/` の stale データの扱い（破棄して新規作成か）も含め未着手
- **左右の Pro Micro（U3/U6）**: 旧「左基板は U6 を除外」等の方針はミラー前提だったため白紙。独立基板として各手にどの Pro Micro/リセットSWを使うか要再整理（電気的には左右対称でネットは同一）

## 設計データに関する注意

- 元の本家データは KiCad 5.0 形式（`kicad_pcb (version 20171130)`）。ソケット作業中の基板は KiCad 10 で保存され **10 形式（version 20260206）**に変換済み。旧リビジョン（Rev.1〜Rev1.2）は 5.0 形式のまま触らない
- ユーザーは KiCad GUI で並行編集する。スクリプト（pcbnew）で基板を書き換える前に、KiCad/エクスプローラがそのフォルダを開いていないか確認すること（開いているとファイルロックで保存・改名が失敗する）。`kicad-cli pcb drc` とネットリスト照合で検証する
- 基板レイアウトを変更したら gerber データも再生成すること。出力先は各基板の `ergodash/`（基板ファイルの plot 設定に保存）。**※現在の gerber は配線やり直し前の状態で stale。配線完了後に再生成する**
- ドキュメントは日本語が一次情報（`Doc/build.md`）、英語版（`Doc/build-en.md`）は追従
